# External qualification packet template

Candidate source/spec/lock/plan digests:
Locally completed work and explicit unresolved items:
Operator / target / authorized actions / secret injection mechanism:
Scenario identity set and execution order:
Resource limits / stop conditions / cleanup:
Evidence schema, raw log storage and public redaction:
Independent evaluator and declared limitations:
Gate/profile claims still NOT_RUN:

Unsigned/unapproved templates do not authorize an external run.
