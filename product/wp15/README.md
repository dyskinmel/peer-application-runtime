# Peer Application Runtime — provisional OSS preview boundary

This is a **Developer Preview / Alpha candidate**, not a public-release authorization. `Peer Application Runtime` is a provisional functional descriptor, not a decided project/brand name.

Final license, project name, security contact, public repository, and release version are intentionally undecided. The public-source boundary is defined by `PUBLIC_PREVIEW_BOUNDARY.json`; internal Git/runtime/recovery evidence and planning metadata are excluded.
